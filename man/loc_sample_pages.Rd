% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/search.R
\name{loc_sample_pages}
\alias{loc_sample_pages}
\title{Sample all items from a \emph{Chronicling America} search}
\usage{
loc_sample_pages(
  query,
  n_sample_page = 10,
  year_start = 1756,
  year_end = 1963,
  facets = NULL,
  ...
)
}
\arguments{
\item{query}{Either a character string or a vector of character strings used
to search pages. The format here should be identical to \link{create_basic_loc_request}.}

\item{n_sample_page}{The number of pages of items to sample each year. Combined
with \code{items_page} will give the sample size of items per year. If the number of
pages returned from the search in a given year is below this number, the full
number of items will be kept. Only full pages are sampled, so the last page
is ignored.}

\item{year_start}{An integer giving the starting year for the search. If not
provided, defaults to earliest date of 1756.}

\item{year_end}{An integer giving the ending year for the search. If not
provided, defaults to latest date of 1963.}

\item{facets}{A set of facets to further restrict the search, as defined in \link{add_facets}.}

\item{...}{Additional parameters that are passed on to \link{create_basic_loc_request}.
Users should not pass the \code{items_page} parameter as this is defined by the
sampling procedure as 1.}
}
\value{
a \link[tibble:tibble]{tibble::tibble} of items, with one item per row.
}
\description{
Sample all items from a \emph{Chronicling America} search, stratified by year, and
place them into a \link[tibble:tibble]{tibble::tibble} dataset.
}
\details{
For very large searches, this function will sample the full results including
a text snippet. Results are sampled by year, so that a consistent number of
items are provided by year. Additionally, sampling is done by whole pages,
so it constitues a form of cluster sampling. Weights and cluster ids are
included in the final result to account for the sampling procedure.

It is best to use year restrictions and/or facets to reduce the total volume
of the search. Searches with more than 100,000 total items will likely be
refused by the API.

The returned results from the json are formatted into a \link[tibble:tibble]{tibble::tibble} with only
the following values:
\itemize{
\item item url
\item date
\item publisher
\item location: county, state, and year
\item text snippet
}

Users who want different values returned would need to adjust the
internal function \code{process_row} which does this work.
}
\examples{

loc_sample_pages("banana", year_start = 1910, year_end = 1915,
                 facets = c(language = "english"))

}
